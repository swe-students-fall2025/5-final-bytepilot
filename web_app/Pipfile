# Pipfile
[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "*"
flask-login = "*"
pymongo = "*"
python-dotenv = "*"
gunicorn = "*"

[dev-packages]
pytest = "*"
pytest-cov = "*"

[requires]
python_version = "3.9"

[scripts]
test = "python -m pytest tests/ -v"
test-cov = "python -m pytest tests/ --cov=app --cov-report=html"